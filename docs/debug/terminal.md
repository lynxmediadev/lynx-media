## Estado
OK — JSX balanceado y compila (`npx tsc --noEmit` dirigido a RightsFormClient.tsx).

## Notas
- Se removió un `</div>` sobrante que cerraba el contenedor principal antes del `<Dialog>`.
- Se mantiene manejo de Enter para evitar submits involuntarios.
